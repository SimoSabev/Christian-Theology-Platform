'use client';
type View = 'matrix' | 'triptych' | 'diff';
type Props = { view: View; onChange: (v: View) => void };
export default function CompareViewSwitcher({ view, onChange }: Props) { return null; }
